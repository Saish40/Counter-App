pipeline {
    agent any
    stages{
        stage('GIT checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/Saish40/Counter-App.git'
            }
        }
        
    }
}